# AP-Key: 프로젝트 서비스 키 관리

> 이 파일에는 프로젝트에서 사용하는 서비스 URL과 인증 키를 기록합니다.
> 교육 시작 전 또는 1주차에 모든 항목을 채워주세요.
>
> **주의**: 이 파일에는 민감한 인증 정보가 포함됩니다.
> 공개 저장소에 Push하지 않도록 `.gitignore`에 추가하세요.

---

## 1. GitHub

| 항목 | 값 |
|------|-----|
| 저장소 URL | `https://github.com/{username}/{repo-name}` |
| Personal Access Token (PAT) | `ghp_xxxxxxxxxxxxxxxxxxxx` |
| PAT 권한 | repo, workflow, project |
| PAT 만료일 | YYYY-MM-DD |

> PAT 발급: GitHub > Settings > Developer settings > Personal access tokens > Tokens (classic)
> `project` 권한은 GitHub Projects 보드 연동에 필요합니다.

---

## 2. Slack

| 항목 | 값 |
|------|-----|
| 워크스페이스 URL | `https://{workspace}.slack.com` |
| 알림 채널 | #{채널명} |
| Incoming Webhook URL | `https://hooks.slack.com/services/T.../B.../xxx` |

> Webhook 설정: Slack > Apps > Incoming Webhooks > Add to Slack > 채널 선택
> Slack Incoming Webhook URL은 n8n Credential이 아닙니다. 워크플로우 내 HTTP Request 노드에 직접 입력합니다.

---

## 3. NotebookLM

| 항목 | 값 |
|------|-----|
| 프로젝트 노트북 URL | `https://notebooklm.google.com/notebook/xxx` |
| Google 계정 | (NotebookLM 로그인용) |
| 등록된 소스 수 | 0 / 목표: 23+ |

> NotebookLM 접속: https://notebooklm.google.com

---

## 4. n8n

> 워크플로우 JSON은 Claude Code가 생성하고, PM이 n8n Cloud에 import + Credential 연결만 수행한다.

| 항목 | 값 |
|------|-----|
| n8n Cloud URL | `https://{instance}.app.n8n.cloud` |

**워크플로우 Webhook**:

| # | 워크플로우 | 용도 | Webhook 경로 |
|---|-----------|------|-------------|
| 1 | GitHub Issue -> Slack 알림 | Issue 생성/변경 시 알림 | /webhook/github-issue-webhook |
| 2 | 주간 요약 -> Slack 전송 | 매주 금요일 진척 요약 | /webhook/weekly-summary |
| 3 | 배포 알림 -> Slack 전송 | CI/CD 완료 시 결과 알림 | /webhook/deploy-notification |
| 4 | Slack AI Agent (Chat UI) | n8n Chat에서 프로젝트 현황 질의 | (Chat Trigger) |
| 5 | Slack PM Agent | Slack 슬래시 명령으로 현황 질의 | /webhook/slack-pm-agent |

**n8n Credentials**:

| Credential | 등록할 키 | 상태 |
|-----------|----------|------|
| GitHub API | PAT (1번 항목) | [ ] |
| Google Gemini API | API Key (WF 04, 05용) | [ ] |

> Slack Incoming Webhook URL은 Credential이 아님. 워크플로우 내 HTTP Request 노드에 직접 입력.

---

## 5. Claude API

| 항목 | 값 |
|------|-----|
| 구독 플랜 | Pro / Max |
| Anthropic API Key | `sk-ant-api03-xxxx` (필요 시 기입) |

> API Key 발급: https://console.anthropic.com > API Keys

---

## 6. Supabase

| 항목 | 값 |
|------|-----|
| 프로젝트 URL | `https://{ref}.supabase.co` |
| DB 비밀번호 | (프로젝트 생성 시 설정한 비밀번호) |
| SUPABASE_KEY (publishable) | `sb_publishable_xxx` |
| service_role | `eyJhbGci...` (JWT) |

> Supabase 대시보드: https://supabase.com/dashboard
> service_role은 백엔드 전용. 프론트엔드에 노출 금지.

---

## 7. 배포

| 항목 | 값 |
|------|-----|
| Frontend (Vercel) | `https://{project}.vercel.app` |
| Backend (Vercel API Routes) | `https://{project}.vercel.app/api` |

> Vercel 대시보드: https://vercel.com/dashboard

### Vercel 환경변수 (Settings > Environment Variables)

> 로컬 `.env.local`의 값은 Vercel이 알지 못한다. 아래 변수를 **Vercel 대시보드에도 동일하게 등록**해야 배포된 사이트가 Supabase/토스와 연결된다. 등록 후 **Redeploy** 필요.
> **명명 규칙**: `NEXT_PUBLIC_` 접두사가 붙은 변수만 브라우저에 노출된다. **시크릿/만능 키에는 절대 `NEXT_PUBLIC_`을 붙이지 않는다.**

| 변수명 | 설명 (값 출처 · 노출 경계) |
|--------|---------------------------|
| `NEXT_PUBLIC_SUPABASE_URL` | DB·Storage 접근 주소 (§6 프로젝트 URL · 공개) |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 공개용 anon 키 (§6 publishable · 공개) |
| `SUPABASE_SERVICE_ROLE_KEY` | 만능 키 (§6 service_role · **서버 전용**) |
| `NEXT_PUBLIC_TOSS_CLIENT_KEY` | 결제창 호출용 클라이언트 키 (§8 · 공개) |
| `TOSS_SECRET_KEY` | 결제 승인(confirm)용 시크릿 키 (§8 · **서버 전용**) |

> **등록 범위**: 각 변수를 Production / Preview / Development 환경에 맞게 등록한다(테스트 키는 Preview·Development, 라이브 키는 Production 분리 권장).
> **인증은 Supabase Auth로 통일**: 회원가입/로그인/세션/소셜로그인을 Supabase가 처리하므로 **자체 `JWT_SECRET`은 필요 없다**(위 SUPABASE 키 재사용).
> **구글 소셜 로그인 키는 Vercel이 아니다**: Google OAuth 클라이언트 ID/Secret은 **Supabase 대시보드(Authentication > Providers > Google)**에 등록한다. 앱/Vercel 환경변수로는 두지 않는다.

---

## 8. PG사 (결제 연동)

> 한국 서비스는 토스페이먼츠 권장 (Stripe는 한국 사업자 미지원).
> 테스트 키로 개발 후, 라이브 키로 전환합니다.

| 항목 | 값 |
|------|-----|
| 클라이언트 키 | `test_ck_xxx` |
| 시크릿 키 | `test_sk_xxx` |
| API 버전 | 2022-11-16 |

> 토스페이먼츠 개발자센터: https://developers.tosspayments.com

---

## 설정 완료 체크리스트

| 항목 | 상태 |
|------|------|
| GitHub 저장소 생성 + PAT 발급 | [ ] |
| Slack 워크스페이스 + Webhook 생성 | [ ] |
| NotebookLM 노트북 생성 + 소스 등록 | [ ] |
| n8n Cloud 가입 + Credentials 등록 | [ ] |
| Supabase 프로젝트 생성 + 키 3종 확보 | [ ] |
| Vercel 배포 + 환경변수 5종 등록 (§7) | [ ] |
| Claude Code 설치 확인 | [ ] |
